services:
  postgres:
    image: postgres:16
    container_name: cafetin_postgres
    restart: always
    environment:
      POSTGRES_USER: cafetinuser
      POSTGRES_PASSWORD: cafetinpass
      POSTGRES_DB: cafetinapp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
